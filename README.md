# To run:

When building from source, run the `makeArduino` bash file.
All the file does it create a new folder with the same name as this directory but prepended with arduino. 
You can then open the newly generated sketch in the Arduino IDE and run it there. 
